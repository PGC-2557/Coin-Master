using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class CoinVerticalAnimation : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Vector3 defaultPosition = transform.position; // coin's default height
        //Vector3 bottomPosition = bottomPosition.Set(defaultPosition.x, defaultPosition.y, defaultPosition.z);
    }

    // Update is called once per frame
    void Update()
    {
       // transform.position = Vector3.Lerp()
    }
}
